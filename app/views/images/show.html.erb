<h1>Images list</h1>
<% if flash[:notice] %>
<div>
  <%= flash[:notice]%>
</div>
<% end %>
<% if Image.blank? %>
  <div>No image is uploaded yet</div>
<% else %>
  <div>
    <% Image.all.each do |image| %>
      <figure>
        <%= link_to image_tag(image.post.url(:thumb)), image.post.url(:original), target: '_blank' %>
        <figcaption><%= image.img_name %></figcaption>
      </figure>
    <% end %>
  </div>
<% end %>

<%= link_to "Back", root_path %>